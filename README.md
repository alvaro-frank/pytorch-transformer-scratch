# pytorch-transformer-scratch
A clean, ground-up implementation of a Decoder-only Transformer in PyTorch. Features custom self-attention mechanisms and efficient tokenization logic. No high-level abstractions used.
